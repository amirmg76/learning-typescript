const jso = '{"x": 10, "y": 20}';
const coordiante = JSON.parse(jso);
console.log(coordiantes);

const new_arr: any[] = [];
new_arr.push(2);
new_arr.push(new Date());
new_arr.push('hello');
console.log(new_arr);